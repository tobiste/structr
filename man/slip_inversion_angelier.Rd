% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_inversion.R
\name{slip_inversion_angelier}
\alias{slip_inversion_angelier}
\title{Stress Inversion for Fault-Slip Data after Angelier (1990)}
\usage{
slip_inversion_angelier(
  x,
  weights = NULL,
  max_iter = 100L,
  tol = 1e-06,
  n_psi = 361L,
  friction = 0.6,
  flip = FALSE
)
}
\arguments{
\item{x}{\code{"Fault"} object where the rows are the observations, and the columns
the coordinates. Must have at least 4 fault measurements.}

\item{weights}{numeric. Weightings for the faults. Must have the same length as \code{x}}

\item{max_iter}{integer. Maximum iteration count (default \code{50}) for Mostafa (2005)
optimization. Set to \code{0} for no optimization.}

\item{tol}{numeric. Convergence tolerance on max absolute change in TR elements between iterations (default \code{1e-6})}

\item{n_psi}{integer. Number of psi grid points for each step (default \code{361})}

\item{friction}{numeric. Coefficient of friction (0.6 by default)}

\item{flip}{logical. Flip if you want to have the negative stress tensor, i.e. sigma 1 and 3 will be flipped.}
}
\value{
a named list with the following components:
\describe{
\item{\code{stress_tensor}}{\code{"ellipsoid"} object. Best-fit devitoric stress tensor in input coordinate frame}
\item{\code{principal_axes}}{\code{"Line"} objects. Orientation of the principal stress axes as unit vectors (max to min)}
\item{\code{tensor_params}}{the four tensor parameters (Eq. 4.87)}
\item{\code{principal_vals}}{eigenvalues of the stress tensor (\eqn{\sigma_1 >= \sigma_2 >= \sigma_3})}
\item{\code{stress_shape}}{list Stress shape ratio. See \code{\link[=stress_shape]{stress_shape()}}.}
\item{\code{misfit}}{list. Misfit parameters. See \code{\link[=slip_inversion_misfit]{slip_inversion_misfit()}}.}
\item{\code{SHmax}}{numeric. Direction of maximum horizontal stress (in degrees)}
\item{\code{tau_mean}}{numeric. Average resolved shear stress on each plane. Should be close to 1.}
\item{\code{stress_components}}{matrix. The resolved shear and normal stresses, the slip and dilation tendency on each plane. See \code{\link[=tau2shearnorm]{tau2shearnorm()}} and \code{\link[=tau2tendency]{tau2tendency()}}.}
\item{\code{n_iter}}{number of Mostafa iterations performed}
}
}
\description{
Direct inversion after the algorithm of Angelier (1990) with
iterative refinement after Mostafa (2005)
}
\details{
The reduced stress tensor (Eq. 4.87) is parameterised as:
\deqn{T_R = \begin{bmatrix}
     \cos(\psi) & d & e \\
     d & \cos(\psi+2\pi/3) & f \\
     e & f & \cos(\psi + 4\pi/3)
   \end{bmatrix}
   }
with two normalisation constraints (Pascal, 2022; Eqs 4.88–4.89):
\enumerate{
\item \eqn{T_{11} + T_{22} + T_{33} = 0}            (deviator)
\item \eqn{T_{11}^2 + T_{22}^2 + T_{33}^2 = 3/2}   (fixes \eqn{\lambda = \sqrt(3)/2})
}

The four unknowns are \eqn{\psi}, \eqn{d}, \eqn{e}, \eqn{f}.

Minimisation function (Eq. 4.101):
\eqn{F_4 = \sum_i \upsilon_i^2},  where \eqn{\upsilon_i = \lambda * \hat{s}_i - \tau_i}

\eqn{dF_4/d(d,e,f)} = 0 yields a 3x3 linear system in (\eqn{d},\eqn{e},\eqn{f}) given \eqn{\psi}.
\eqn{dF_4/d\psi = 0} is nonlinear; solved here by grid search + Brent refinement.

Mostafa (2005) replaces the global \eqn{\lambda} with per-fault \eqn{\lambda_i} equal to
the shear traction magnitude on each plane and iterates until convergence.
}
\note{
The solution can be refined iteratively by weighting the faults using the RUP values.
This could be done using \code{\link[=scale_weights]{scale_weights()}} which scales the RUP values:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{ # run a first inversion:
 first <- slip_inversion_angelier(x)
 first$

 # in the
 second <- slip_inversion_angelier(x, weights = scale_weights(first$misfit$rup, error_type = 'rup'))
 print(second)
}\if{html}{\out{</div>}}
}
\examples{
nx <- length(angelier1990)
par(mfrow = c(1, nx))

invisible(lapply(seq_len(nx), function(i) {
  # inversion
  x <- angelier1990[[i]]
  res <- slip_inversion_angelier(x, max_iter = 0)

  # some stress shape
  phi_val <- round(res$stress_shape$phi, 2)

  # misfit
  rup_val <- round(res$misfit$rup_mean, 2)

  # Plot the faults (color-coded by RUP\%) and show the principal stress axes
  stereoplot(title = names(angelier1990)[i], guides = FALSE)
  stereo_shmax(res$SHmax)
  fault_plot(x, col = assign_col(res$misfit$rup))
  points(res$principal_axes, col = 1:3, pch = 16, cex = 1.5)
  text(res$principal_axes,
    label = rownames(res$principal_axes),
    col = 1:3, adj = -.25
  )
  legend("topleft", col = 2:4, legend = rownames(res$principal_axes), pch = 16)
  title(sub = bquote(Phi == .(phi_val) ~ "|" ~ bar("RUP") == .(rup_val) * "\%"))
}))
}
\references{
Angelier, J. (1990). Inversion of field data in fault tectonics to obtain the
regional stress—III. A new rapid direct inversion method by analytical means.
Geophys. J. Int, 103, 363–376. \url{https://doi.org/10.1111/j.1365-246X.1990.tb01777.x}

Pascal, C. (2022). Paleostress Inversion Techniques. Chapter 4, Sections 4.2.3 and 4.2.4.

Mostafa, M. E. (2005). Iterative direct inversion: An exact complementary
solution for inverting fault-slip data to obtain palaeostresses. Computers
& Geosciences, 31(8), 1059–1070. \url{https://doi.org/10.1016/j.cageo.2005.02.012}
}
\seealso{
Other stress-inversion: 
\code{\link{Fault_PT}()},
\code{\link{slip_inversion}()},
\code{\link{slip_inversion_hansen}()},
\code{\link{slip_inversion_hansen_boot}()},
\code{\link{slip_inversion_michael}()},
\code{\link{slip_inversion_simple}()}
}
\concept{stress-inversion}
