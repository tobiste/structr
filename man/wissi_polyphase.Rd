% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_inversion_wissi.R
\name{wissi_polyphase}
\alias{wissi_polyphase}
\title{Polyphase stress inversion via spectral clustering on \eqn{S^5} (Stage 5).}
\usage{
wissi_polyphase(
  normals,
  slips,
  weights = NULL,
  k_max = 4L,
  sigma_K_deg = 30,
  seed = NULL,
  ...
)
}
\arguments{
\item{weights}{numeric. Weightings for the faults. Must have the same length as \code{x}}

\item{k_max}{Maximum number of phases to consider. Default 4.}

\item{sigma_K_deg}{Affinity bandwidth in degrees of angular stress
distance. Faults within this distance are considered
similar. Default 30. Increase to merge nearby phases,
decrease to split them.}

\item{seed}{Optional RNG seed for k-means reproducibility.}

\item{...}{Additional arguments passed to wissi() for each phase.}
}
\value{
A named list with:
assignment    : integer vector of length N (phase label 1..k per fault)
k_opt         : number of phases identified
gaps          : Laplacian eigenvalue gaps (eigengap criterion)
phase_results : list of k wissi() results, one per phase
D_mat         : N x N pairwise ASD matrix between fault poles (degrees)
}
\description{
Identifies k stress phases automatically using the eigengap heuristic,
then runs wissi() on each phase subset.
}
\examples{
res <- wissi_polyphase(angelier1990$TYM)
res$k_opt
}
