% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/best_pole.R
\name{regression-gray}
\alias{regression-gray}
\alias{regression_gray}
\alias{regression_smallcircle_gray}
\alias{regression_greatcircle_gray}
\title{Least-Squares Small and Great Circle Fit for Spherical Data}
\usage{
regression_gray(
  x,
  out = c("great", "small", "both"),
  sigma2 = 10,
  tol = NULL,
  max_iter = 100,
  verbose = FALSE
)

regression_smallcircle_gray(
  x,
  sigma2 = 10,
  tol = NULL,
  max_iter = 100,
  verbose = FALSE
)

regression_greatcircle_gray(
  x,
  sigma2 = 10,
  tol = NULL,
  max_iter = 100,
  verbose = FALSE
)
}
\arguments{
\item{x}{object of class \code{"Vec3"}, \code{"Line"}, \code{"Ray"}, or \code{"Plane"}, where the
rows are the observations and the columns are the coordinates.}

\item{out}{character. Type of regression. \code{"small"} for a small-circle fit, \code{"great"} for the great-circle fit, and \code{"both"} for small- and great-circle estimates.}

\item{sigma2}{numeric. Known error in degrees for a goodness-of-fit test. Default is \code{10} °}

\item{tol}{numeric. Machine precision. Defaults to \code{getOption("structr.tol")}.}

\item{max_iter}{integer. Number of Newton-Lagrange optimizations. Default is \code{100}}

\item{verbose}{logical. Whether results of the iteration should be printed during the calculation. Default is \code{FALSE}}
}
\value{
list. \describe{
\item{\code{vec}}{\code{Line} of \code{Vec3} object, sthe axis of the circle solution}
\item{\code{cone}}{Half-apical angle of the small-circle in degrees. Alway 90 for a great-circle solution.}
\item{\code{r_squared}}{Residual sum of squares}
\item{\code{residuals}}{Per-point residuals}
\item{\code{iterations}, \code{converged}, \code{p}}{Newton-iteration diagnostics}
\item{\code{type}}{character. Type of the solution}
\item{\code{statistic}, \code{df}, \code{p_value}}{Statistic, degrees of freedom and p-value of the \eqn{\chi^2} goodness-of-fit test}
}

If \verb{out = 'both}, the a comparison of both solutions is added to the output:
\describe{
\item{\code{V_r}}{Test statistic \eqn{V_r}}
\item{\verb{df1, df2}}{Degrees of freedom of the small and the great circle data, respectively}
\item{\code{p_value}}{P-value of the comparison test}
}
}
\description{
Finds the best small and great circles using the algorithm by Gray et al. (1980)
}
\details{
\subsection{Goodness-of-fit test}{

\eqn{\chi^2} goodness-of-fit check against an independently known error
variance \eqn{\sigma^2}
\deqn{E(\sum R_i^2) = (p-q) \sigma^2}, \eqn{q=3} (great
circle) or \eqn{q=4} (small circle).
}

\subsection{Test whether a small-circle fit is a significant improvement over the}{

corresponding great-circle fit

\deqn{V_r = (p-3)  [(r_g - r_c) / r_c] ~ F(1, p-3)} under the paper's
normal-residual assumptions.
}
}
\examples{
data("gray_example")

# regression:
best_clea <- regression_gray(gray_example[1:8, ], out = "both")
best_bedd <- regression_gray(gray_example[9:16, ], out = "both")
best_all <- regression_gray(gray_example, out = "both")

# R-squared values:
best_clea$great$r_squared
best_bedd$great$r_squared
best_all$great$r_squared

stereoplot()
points(gray_example[1:8, ], col = "blue")
points(gray_example[9:16, ], col = "red", pch = "x")

# best for cleavage
lines(best_clea$small$vec, best_clea$small$cone, col = "lightblue")
lines(best_clea$great$vec, 90, lty = 2, col = "blue")

# best for bedding
lines(best_bedd$small$vec, best_bedd$small$cone, col = "sienna")
lines(best_bedd$great$vec, 90, lty = 2, col = "red")

# best for all
lines(best_all$small$vec, best_bedd$small$cone, col = "gray70")
lines(best_all$great$vec, 90, lty = 2, col = "gray60")

# Compare small and great-circle solution:
regression_gray(gray_example[1:8, ], out = "both")$comparison
regression_gray(gray_example[9:16, ], out = "both")$comparison
regression_gray(gray_example, out = "both")$comparison
}
\references{
Gray, N.H., Geiser, P.A., Geiser, J.R. (1980). On the
least-square fit of small and great circles to spherically projected data.
Mathematical Geology, Vol. 12, No. 3, 1980.
}
\seealso{
\code{\link[=regression_smallcircle]{regression_smallcircle()}}, \code{\link[=regression_greatcircle]{regression_greatcircle()}}
}
