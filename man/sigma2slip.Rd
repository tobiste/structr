% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_inversion.R
\name{sigma2slip}
\alias{sigma2slip}
\title{Predict Slip Direction for Given Faults and Stress Tensor}
\usage{
sigma2slip(sigma, normals, tol = NULL)
}
\arguments{
\item{sigma}{matrix of object of class \code{"ellipsoid"}. 3x3 reduced stress tensor
(as returned by \code{sigma} from \code{\link[=slip_inversion]{slip_inversion()}})}

\item{normals}{object of class \code{"Plane"} of \code{"Vec3"}}

\item{tol}{numeric. Convergence tolerance on max absolute change in TR elements between iterations. Defaults to \code{getOption("structr.tol")}.}
}
\value{
list. \describe{
\item{\code{slip_dirs}}{\code{Vec3} or \code{Ray} object of predicted slip direction vectors}
\item{\code{tau_mag}}{length-N vector of shear traction magnitudes \eqn{|\tau|} (unnormalised)}
\item{\code{degenerate}}{logical. \code{TRUE} where \eqn{|\tau|} < \code{tol} (plane contains a
principal stress axis — slip direction undefined)}
}
}
\description{
Forward problem: given a reduced stress tensor and one or more fault planes,
predict the slip direction(s) under the Wallace-Bott hypothesis.
}
\examples{
sigma <- reduced_stress(angelier1990$AVB)
normals <- Plane(120, 50)
s <- sigma2slip(sigma, normals)
print(s)

plot(normals)
pr <- sigma2stress(sigma)
points(pr$axes, col = 2:4, pch = 16)
text(pr$axes, label = rownames(pr$axes), col = 2:4, adj = -.25)
stereo_arrows(s$slip_dirs, sense = -sign(s$slip_dirs[, 2]))
}
\seealso{
\code{\link[=slip_inversion]{slip_inversion()}}; \code{\link[=stereo_stress_grid]{stereo_stress_grid()}}
}
