% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_inversion_wissi.R
\name{slip_inversion_wissi_boot}
\alias{slip_inversion_wissi_boot}
\title{Bootstrap uncertainty for a WISSI result.}
\usage{
slip_inversion_wissi_boot(x, n_iter = 500L, seed = NULL, ...)
}
\arguments{
\item{x}{object of class \code{"Pair"} or \code{"Fault"} with at least 4 rows.}

\item{n_iter}{Number of bootstrap replicates. Default \code{500}.}

\item{seed}{Optional RNG seed.}

\item{...}{Additional arguments passed to \code{\link[=slip_inversion_wissi]{slip_inversion_wissi()}}.}
}
\value{
A named list with:
\describe{
\item{\code{optimal}}{slip_inversion_wissi() result for the full dataset}
\item{\code{thetas}}{length-\code{n_iter} vector of angular stress distances from optimal}
\item{\code{dispersion}}{mean Theta (approximates noise level p of data)}
\item{\code{sd}}{standard deviation of Theta values}
\item{\code{D_bar}}{mean Orife-Lisle distance from optimal}
\item{\code{DM_bar}}{mean Michael distance from optimal}
}
}
\description{
Yields \code{n_iter} stress tensors from resampled datasets. The dispersion
Theta-bar on \eqn{S^5} approximates the data noise level (Eq. 37: Theta ~ d-bar).
}
\examples{
res <- slip_inversion_wissi_boot(angelier1990$AVB, n_iter = 4)

stereoplot()
angelier(angelier1990$KAM, col = 'grey')
lines(res$optimal$principal_axes, res$sd, col = 2:4)
points(res$optimal$principal_axes, pch = 16:18, cex = 2, col= 2:4)
text(res$optimal$principal_axes, 
label = rownames(res$optimal$principal_axes), col= 2:4, adj = -.25)
}
\seealso{
Other wissi: 
\code{\link{slip_inversion_wissi}()},
\code{\link{slip_inversion_wissi_polyphase}()}
}
\concept{wissi}
