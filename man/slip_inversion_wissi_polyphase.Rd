% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stress_inversion_wissi.R
\name{slip_inversion_wissi_polyphase}
\alias{slip_inversion_wissi_polyphase}
\title{Polyphase stress inversion via spectral clustering on \eqn{S^5} (Stage 5).}
\usage{
slip_inversion_wissi_polyphase(
  x,
  weights = NULL,
  k_max = 4L,
  sigma_K_deg = 30,
  seed = NULL,
  ...
)
}
\arguments{
\item{x}{\code{"Fault"} object where the rows are the observations, and the
columns the coordinates. Object must be complete, i.e. no \code{NA} values.
For Michael's, Angelier's, and Yamaji-Sato's methods, at least 4 rows
of fault measurements are required, while Hansen's method requires at least 7.}

\item{weights}{Optional length-\code{N} numeric vector of non-negative data
quality weights. \code{NA} values are replaced with the observed mean
before scaling (neutral imputation). The vector is normalised internally
so that \code{mean(weights) = 1}, meaning only the ratios between weights
matter. Use \code{\link[=scale_weights]{scale_weights()}} to construct weights from field quality
ranks, measurement errors, and prior RUP values. Default: uniform
weights.}

\item{k_max}{Maximum number of phases to consider. Default \code{4}.}

\item{sigma_K_deg}{Affinity bandwidth in degrees of angular stress
distance. Faults within this distance are considered
similar. Default \code{30.} Increase to merge nearby phases,
decrease to split them.}

\item{seed}{Optional RNG seed for k-means reproducibility.}

\item{...}{Additional arguments passed to \code{slip_inversion_wissi()} for each phase.}
}
\value{
A named list with: \describe{
\item{\code{assignment}}{integer vector of length N (phase label 1..k per fault)}
\item{\code{k_opt}}{number of phases identified}
\item{\code{gaps}}{Laplacian eigenvalue gaps (\emph{eigengap} criterion)}
\item{\code{phase_results}}{list of \code{k} \code{slip_inversion_wissi()} results, one per phase}
\item{\code{D_mat}}{N x N pairwise ASD matrix between fault poles (degrees)}
}
}
\description{
Identifies \code{k} stress phases automatically using the \emph{eigengap heuristic},
then runs \code{slip_inversion_wissi()} on each phase subset.
}
\examples{
res <- slip_inversion_wissi_polyphase(angelier1990$KAM, sigma_K_deg = 15)

# check amount of clusters detected
res$k_opt


# plot the results in lambert projection:
cols <- assign_col_d(seq_len(res$k_opt))#' 
par(mfrow = c(1, 2))

stereoplot()
angelier(angelier1990$KAM, col = assign_col_d(res$assignment))
legend('topleft', title = 'Phases', fill = cols, legend = 1:3)


stereoplot()
angelier(angelier1990$KAM, col = 'grey')
for(k in seq_len(res$k_opt)){
   res_k <- res$phase_results[[k]]
   points(res_k$principal_axes, col = cols[k], pch = 16:18, cex = 2) 
 }
legend('topright', title = "Principal axes", pch = 16:18, col = 'black', 
   legend = c('S1', 'S2', 'S3'))
 
 dev.off()
}
\seealso{
Other wissi: 
\code{\link{slip_inversion_wissi}()},
\code{\link{slip_inversion_wissi_boot}()}
}
\concept{wissi}
