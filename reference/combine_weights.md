# Combine multiple weight vectors multiplicatively and normalise.

Each element of ... is a numeric vector of length N. Returns a
normalised weight vector (mean = 1).

## Usage

``` r
combine_weights(...)
```
